$("#teach-assign-index").on("ajax:success", checkForAssignments())

function checkForAssignments() {
  if( $("#<%= @cohort.id %>-assign-partial").length ) {
    hideAssignments()
  } else {
    showAssignments()
  }
}

function showAssignments() {
  $("<%= j render 'partials/teachers/teachassignindex' %>").insertAfter("#cohort-<%=@cohort.id%>-teach-show")
}

function hideAssignments() {
  $("#<%= @cohort.id %>-assign-partial").remove()
}